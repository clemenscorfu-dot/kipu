# Kipu brand master

The visual reference is the approved ChatGPT-generated branding sheet. Do not redesign or reinterpret the mark.

Canonical mark: `kipu-icon-master.svg`.

Geometry: muted sage rounded square (`#6f9d91`) with the white knotted lowercase-k mark. The knot is part of the k, not a separate decorative symbol.

For PWA production assets, render this master without changing geometry, stroke width, colors, padding, or corner radius. Required outputs: 16, 32, 48, 180, 192, 512 PNG. Apple touch icon uses 180x180. Maskable variants should preserve the same mark inside the safe zone; do not redraw it.

Wordmark should visually match the approved reference: rounded lowercase `kipu`, charcoal `#273036`; when used as a compact brand lockup the k may use sage `#6f9d91`.
