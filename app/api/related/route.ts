import {NextRequest,NextResponse} from "next/server";import{createClient}from"@supabase/supabase-js";
export const maxDuration=45;
const schema={type:"object",additionalProperties:false,properties:{items:{type:"array",maxItems:4,items:{type:"object",additionalProperties:false,properties:{id:{type:"string"},reason:{type:"string"}},required:["id","reason"]}}},required:["items"]};
export async function POST(req:NextRequest){try{const auth=req.headers.get("authorization");if(!auth?.startsWith("Bearer "))return NextResponse.json({error:"unauthorized"},{status:401});const token=auth.slice(7),url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,openAiKey=process.env.OPENAI_API_KEY;if(!url||!key||!openAiKey)return NextResponse.json({error:"missing_config"},{status:500});const s=createClient(url,key,{global:{headers:{Authorization:`Bearer ${token}`}},auth:{persistSession:false,autoRefreshToken:false}});const{data:{user}}=await s.auth.getUser(token);if(!user)return NextResponse.json({error:"unauthorized"},{status:401});const body=await req.json(),ideaId=String(body?.idea_id??"");if(!ideaId)return NextResponse.json({error:"idea_id_required"},{status:400});const{data,error}=await s.from("ideas").select("id,title,summary,original_input,tags,people,location_label,created_at,enrichment").eq("user_id",user.id).order("created_at",{ascending:false}).limit(250);if(error)throw error;const ideas=(data??[]).filter((x:any)=>x.enrichment?.processing_status!=="merged"),current=ideas.find((x:any)=>x.id===ideaId);if(!current)return NextResponse.json({error:"not_found"},{status:404});const candidates=ideas.filter((x:any)=>x.id!==ideaId);if(!candidates.length)return NextResponse.json({items:[]});const compact=(x:any)=>({id:x.id,title:x.title,summary:x.summary,original:x.original_input,tags:x.tags,people:x.people,location:x.location_label,category:x.enrichment?.category,facts:x.enrichment?.facts});const r=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{Authorization:`Bearer ${openAiKey}`,"Content-Type":"application/json"},body:JSON.stringify({model:"gpt-5.6-luna",input:[{role:"system",content:[{type:"input_text",text:"Du bist Kipu, ein persönliches Gedächtnis. Finde bis zu 4 wirklich sinnvolle verwandte Erinnerungen zur aktuellen Erinnerung. Beziehungen können Thema, Person/Autor, Ort, Vorhaben, Anlass oder semantischer Zusammenhang sein. Wähle nicht bloß deshalb etwas aus, weil es dieselbe grobe Kategorie hat. Wenn nichts wirklich passt, gib eine leere Liste zurück. Begründe jede Verbindung in höchstens 8 deutschen Wörtern."}]},{role:"user",content:[{type:"input_text",text:`Aktuell: ${JSON.stringify(compact(current))}\n\nAndere Erinnerungen: ${JSON.stringify(candidates.map(compact))}`}]}],text:{format:{type:"json_schema",name:"related_memories",strict:true,schema}}})});if(!r.ok)throw new Error(`openai_${r.status}_${await r.text()}`);const payload:any=await r.json();let raw=payload.output_text;if(!raw)for(const o of payload.output??[])for(const c of o.content??[])if(typeof c?.text==="string")raw=c.text;if(!raw)throw new Error("no_related_output");const parsed=JSON.parse(raw),wanted=new Map((parsed.items??[]).map((x:any)=>[x.id,x.reason]));const items=candidates.filter((x:any)=>wanted.has(x.id)).map((x:any)=>({id:x.id,title:x.title,summary:x.summary,location_label:x.location_label,enrichment:x.enrichment,reason:wanted.get(x.id)}));return NextResponse.json({items});}catch(e){console.error("related memories failed",e);return NextResponse.json({error:e instanceof Error?e.message:"related_failed"},{status:500})}}
