import{NextRequest,NextResponse}from"next/server";import{createClient}from"@supabase/supabase-js";import{indexIdeaMemory}from"@/lib/kipu-memory-index";export const maxDuration=60;
export async function POST(req:NextRequest){try{const auth=req.headers.get("authorization");if(!auth?.startsWith("Bearer "))return NextResponse.json({error:"unauthorized"},{status:401});const token=auth.slice(7),url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,openAiKey=process.env.OPENAI_API_KEY;if(!url||!key||!openAiKey)return NextResponse.json({error:"missing_config"},{status:500});const s=createClient(url,key,{global:{headers:{Authorization:`Bearer ${token}`}},auth:{persistSession:false,autoRefreshToken:false}}),{data:{user}}=await s.auth.getUser(token);if(!user)return NextResponse.json({error:"unauthorized"},{status:401});const{data,error}=await s.from("ideas").select("id,enrichment").eq("user_id",user.id).order("created_at",{ascending:false}).limit(100);if(error)throw error;const pending=(data??[]).filter((x:any)=>x.enrichment?.processing_status==="ready"&&!x.enrichment?.memory).slice(0,8);let indexed=0;for(const x of pending){try{await indexIdeaMemory(s,user.id,openAiKey,x.id);indexed++}catch(e){console.error("memory backfill item failed",x.id,e)}}return NextResponse.json({indexed,remaining:Math.max(0,(data??[]).filter((x:any)=>x.enrichment?.processing_status==="ready"&&!x.enrichment?.memory).length-indexed)});}catch(e){console.error("memory backfill failed",e);return NextResponse.json({error:e instanceof Error?e.message:"backfill_failed"},{status:500})}}
