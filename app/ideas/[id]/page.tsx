"use client";
import{ArrowLeft,ChevronDown,ExternalLink,Info,LoaderCircle,MapPin,Pencil,Share2,Sparkles,Trash2}from"lucide-react";import Link from"next/link";import{useParams,useRouter}from"next/navigation";import{useEffect,useMemo,useState}from"react";import{mockIdeas}from"@/lib/mock-ideas";import{ensureAnonymousSession,getSupabaseBrowserClient}from"@/lib/supabase-browser";
type Enrichment={category?:string;facts?:Array<{label:string;value:string}>;sources?:Array<{title?:string;url:string}>;useful_links?:Array<{label:string;url:string}>;subject_coordinates?:{latitude:number;longitude:number}|null;image_url?:string|null;image_fit?:"cover"|"contain";processing_status?:"pending"|"processing"|"ready"|"failed"|"merged";merged_into_idea_id?:string};type StoredIdea={id:string;original_input:string;title:string;summary:string|null;tags:string[];location_label:string|null;enrichment:Enrichment;created_at:string};const uuid=/^[0-9a-f]{8}-[0-9a-f-]{27}$/i;
function captureLocationExplicit(text:string){const value=text.toLocaleLowerCase("de-CH").replace(/\s+/g," ").trim();return /\b(hier|genau hier|an diesem ort|an dieser stelle|diese stelle|dieser ort|da wo ich bin|dort wo ich bin|mein aktueller standort)\b/i.test(value)||/\b(dies(?:e|er|es)\s+[^.!?]{0,35}\s+hier)\b/i.test(value)}function inputMentionsLocation(text:string,location:string|null){if(!location)return false;const t=text.toLocaleLowerCase("de-CH");return location.toLocaleLowerCase("de-CH").split(/[^\p{L}\p{N}]+/u).filter(w=>w.length>=3).some(w=>t.includes(w))}
function KipuThinking(){return <div className="kipu-thinking"><span className="kipu-orbit kipu-orbit-one"/><span className="kipu-orbit kipu-orbit-two"/><span className="kipu-spark kipu-spark-one">✦</span><span className="kipu-spark kipu-spark-two">✧</span><div className="kipu-bot"><span className="kipu-antenna"/><span className="kipu-ear kipu-ear-left"/><span className="kipu-ear kipu-ear-right"/><span className="kipu-face"><i/><i/></span></div><span className="kipu-thinking-label">Kipu denkt nach</span></div>}
export default function IdeaDetailPage(){const{id}=useParams<{id:string}>(),router=useRouter();const[stored,setStored]=useState<StoredIdea|null>(null),[loading,setLoading]=useState(uuid.test(id)),[error,setError]=useState<string|null>(null),[more,setMore]=useState(false),[imgFailed,setImgFailed]=useState(false),[deleting,setDeleting]=useState(false);const mock=useMemo(()=>mockIdeas.find(x=>x.id===id)??mockIdeas[0],[id]);useEffect(()=>{if(!uuid.test(id))return;let active=true,timer:ReturnType<typeof setInterval>|null=null;async function load(){try{await ensureAnonymousSession();const{data,error}=await getSupabaseBrowserClient().from("ideas").select("*").eq("id",id).maybeSingle();if(error)throw error;if(!data){if(active){setError("Idee konnte nicht geladen werden.");setLoading(false)}return}const next=data as StoredIdea;if(next.enrichment?.processing_status==="merged"&&next.enrichment.merged_into_idea_id){router.replace(`/ideas/${next.enrichment.merged_into_idea_id}`);return}if(active){setStored(next);setError(null);const s=next.enrichment?.processing_status;if((s==="pending"||s==="processing")&&!timer)timer=setInterval(()=>void load(),2000);if(s!=="pending"&&s!=="processing"&&timer){clearInterval(timer);timer=null}}}catch(e){if(active)setError(e instanceof Error?e.message:"Idee konnte nicht geladen werden.")}finally{if(active)setLoading(false)}}void load();return()=>{active=false;if(timer)clearInterval(timer)}},[id,router]);async function remove(){if(!uuid.test(id)||!confirm("Diese Erinnerung wirklich löschen?"))return;setDeleting(true);const{error}=await getSupabaseBrowserClient().from("ideas").delete().eq("id",id);if(error){alert("Löschen fehlgeschlagen: "+error.message);setDeleting(false);return}router.replace("/ideas")}async function share(title:string,summary:string|null){try{if(navigator.share)await navigator.share({title,text:summary||title,url:location.href});else await navigator.clipboard.writeText(location.href)}catch{}}if(loading)return <main className="mx-auto flex min-h-[100dvh] max-w-[430px] items-center justify-center bg-[#fbfaf7]"><LoaderCircle className="animate-spin text-[#79aa36]"/></main>;if(error&&uuid.test(id))return <main className="mx-auto min-h-[100dvh] max-w-[430px] bg-[#fbfaf7] p-4"><Link href="/ideas"><ArrowLeft className="h-5 w-5"/></Link><p className="mt-6 text-sm">{error}</p></main>;
const title=stored?.title??mock.title,summary=stored?.summary??mock.summary,original=stored?.original_input??mock.originalInput,tags=(stored?.tags??mock.tags).slice(0,3),rawLocation=stored?.location_label??mock.location?.label??null,enrichment=stored?.enrichment??{},status=enrichment.processing_status,processing=status==="pending"||status==="processing",failed=status==="failed",category=enrichment.category,facts=enrichment.facts??[],sources=enrichment.sources??[],links=enrichment.useful_links??[],image=enrichment.image_url,fit=enrichment.image_fit??"cover",created=stored?.created_at?new Date(stored.created_at).toLocaleString("de-CH",{dateStyle:"short",timeStyle:"short"}):"",emoji=category==="Restaurant"?"🍽️":category==="Produkt"?"📦":category==="Buch"?"📚":category==="Idee"?"💡":"📌";const locationLabel=(enrichment.subject_coordinates||captureLocationExplicit(original)||inputMentionsLocation(original,rawLocation))?rawLocation:null,primaryLinks=links.filter(l=>!l.url.includes("google.com/maps")).slice(0,2),mapLink=links.find(l=>l.url.includes("google.com/maps")),hiddenSources=sources.filter(s=>!links.some(l=>l.url===s.url)).slice(0,4);
return <main className="mx-auto min-h-[100dvh] w-full max-w-[430px] bg-[#fbfaf7] text-[#111]"><header className="flex items-center justify-between px-4 py-4"><div className="flex items-center gap-3"><Link href="/ideas" aria-label="Zurück" className="flex h-9 w-9 items-center justify-center rounded-full bg-white shadow-[0_4px_14px_rgba(0,0,0,.06)]"><ArrowLeft className="h-4 w-4"/></Link><span className="text-[12px] font-medium text-black/45">Erinnerung</span></div><div className="flex items-center gap-2"><button onClick={()=>void share(title,summary)} aria-label="Teilen" className="flex h-9 w-9 items-center justify-center rounded-full bg-white shadow-[0_4px_14px_rgba(0,0,0,.05)]"><Share2 className="h-4 w-4"/></button>{uuid.test(id)&&<button onClick={remove} disabled={deleting} aria-label="Löschen" className="flex h-9 w-9 items-center justify-center rounded-full bg-white text-[#b94b43] shadow-[0_4px_14px_rgba(0,0,0,.05)]">{deleting?<LoaderCircle className="h-4 w-4 animate-spin"/>:<Trash2 className="h-4 w-4"/>}</button>}</div></header><div className="relative mx-4 flex h-[220px] items-center justify-center overflow-hidden rounded-[24px] bg-[#e5eadf]">{processing?<KipuThinking/>:image&&!imgFailed?<img src={image} alt={title} onError={()=>setImgFailed(true)} className={`h-full w-full ${fit==="contain"?"object-contain p-4":"object-cover"}`} referrerPolicy="no-referrer"/>:<span className="text-[58px]">{emoji}</span>}{!processing&&<button aria-label="Bild bearbeiten" className="absolute bottom-3 right-3 flex h-9 w-9 items-center justify-center rounded-full bg-white/95 shadow"><Pencil className="h-4 w-4"/></button>}</div><section className="px-4 pb-8 pt-5">{processing&&<div className="mb-4 flex items-center gap-3 rounded-[16px] bg-[#eef5e5] px-4 py-3 text-[#65942c]"><Sparkles className="h-5 w-5"/><div><p className="text-[12px] font-semibold">✓ Gemerkt</p><p className="mt-.5 text-[10px] text-black/45">Kipu baut deine Erinnerung gerade zusammen.</p></div></div>}{failed&&<div className="mb-4 rounded-[14px] bg-[#fff2e8] px-4 py-3 text-[11px] text-[#8a5b31]">Gespeichert · Aufbereitung noch nicht abgeschlossen.</div>}<h1 className="text-[25px] font-semibold leading-[1.12] tracking-[-.025em]">{title}</h1>{locationLabel&&<p className="mt-2 flex items-center gap-2 text-[11px] text-black/50"><MapPin className="h-4 w-4 text-[#79aa36]"/>{locationLabel}</p>}<div className="mt-3 flex flex-wrap gap-2">{tags.map(t=><span key={t} className="rounded-full bg-[#f0efeb] px-3 py-1.5 text-[10px] text-black/60">{t}</span>)}</div>{mapLink&&<a href={mapLink.url} target="_blank" rel="noreferrer" className="mt-3 inline-flex items-center gap-1.5 rounded-full bg-[#dff1c9] px-3 py-2 text-[10px] font-semibold text-[#65942c]"><MapPin className="h-3.5 w-3.5"/>Auf Karte ansehen</a>}<div className="kipu-card mt-4 p-4"><p className="text-[10px] font-semibold uppercase tracking-[.08em] text-black/35">Warum gespeichert</p><p className="mt-2 text-[13px] leading-5">{original}</p>{created&&<p className="mt-2 text-[9px] text-black/35">{created}</p>}</div>{!processing&&<div className="kipu-card mt-3 p-4"><h2 className="text-[13px] font-semibold">Kurz dazu</h2><p className="mt-2 text-[13px] leading-5 text-black/75">{summary}</p>{facts.length>0&&<dl className="mt-3 divide-y divide-black/5">{facts.slice(0,4).map((f,i)=><div key={i} className="grid grid-cols-[88px_1fr] gap-3 py-2.5 text-[11px]"><dt className="text-black/40">{f.label}</dt><dd className="font-medium leading-4">{f.value}</dd></div>)}</dl>}{primaryLinks.length>0&&<div className="mt-4 flex flex-wrap gap-2">{primaryLinks.map((l,i)=><a key={i} href={l.url} target="_blank" rel="noreferrer" className="flex items-center gap-1.5 rounded-full bg-[#eef5e5] px-3 py-2 text-[10px] font-medium text-[#65942c]"><ExternalLink className="h-3.5 w-3.5"/>{l.label}</a>)}</div>}{(facts.length>4||hiddenSources.length>0)&&<><button onClick={()=>setMore(!more)} className="mt-4 flex items-center gap-1.5 text-[10px] text-black/45"><Info className="h-3.5 w-3.5"/>Quellen & Details<ChevronDown className={`h-3.5 w-3.5 transition ${more?"rotate-180":""}`}/></button>{more&&<div className="mt-3 border-t border-black/5 pt-2">{hiddenSources.map((s,i)=><a key={i} href={s.url} target="_blank" rel="noreferrer" className="mt-2 flex gap-2 text-[10px] leading-4 text-black/50"><ExternalLink className="mt-.5 h-3 w-3 shrink-0"/>{s.title||s.url}</a>)}</div>}</>}</div>}</section></main>}
