"use client";
import {ArrowDownAZ,CalendarArrowDown,CalendarArrowUp,Check,ChevronDown,Filter,Home,LoaderCircle,Search,Sparkles,Trash2,UserRound,X} from "lucide-react";import Link from "next/link";import {useEffect,useMemo,useRef,useState} from "react";import {ensureAnonymousSession,getSupabaseBrowserClient} from "@/lib/supabase-browser";
type IdeaRow={id:string;title:string;summary:string|null;tags:string[];location_label:string|null;created_at:string;enrichment:{category?:string;image_url?:string|null;image_fit?:"cover"|"contain";processing_status?:"pending"|"processing"|"ready"|"failed"|"merged"}};type CategoryRow={id:string;parent_id:string|null;name:string;sort_order:number};type AssignmentRow={idea_id:string;category_id:string};type SortMode="newest"|"oldest"|"az";function emojiFor(c?:string){if(c==="Buch")return"📚";if(c==="Restaurant"||c==="Essen")return"🍽️";if(c==="Produkt")return"📦";if(c==="Idee")return"💡";if(c==="Film")return"🎬";return"📌"}
export default function IdeasPage(){const[ideas,setIdeas]=useState<IdeaRow[]>([]),[categories,setCategories]=useState<CategoryRow[]>([]),[assignments,setAssignments]=useState<AssignmentRow[]>([]),[loading,setLoading]=useState(true),[deleting,setDeleting]=useState(false),[organizing,setOrganizing]=useState(false),[filterOpen,setFilterOpen]=useState(false),[sort,setSort]=useState<SortMode>("newest");const[error,setError]=useState<string|null>(null),[selected,setSelected]=useState("all");const curatorStarted=useRef(false);useEffect(()=>{let active=true,timer:ReturnType<typeof setTimeout>|null=null;async function load(){try{await ensureAnonymousSession();const s=getSupabaseBrowserClient();const[i,c,a]=await Promise.all([s.from("ideas").select("id,title,summary,tags,location_label,created_at,enrichment").order("created_at",{ascending:false}),s.from("categories").select("id,parent_id,name,sort_order").order("sort_order").order("name"),s.from("idea_categories").select("idea_id,category_id")]);if(i.error)throw i.error;if(c.error)throw c.error;if(a.error)throw a.error;if(!active)return;const next=((i.data??[]) as IdeaRow[]).filter(x=>x.enrichment?.processing_status!=="merged"),nextCategories=(c.data??[]) as CategoryRow[],nextAssignments=(a.data??[]) as AssignmentRow[];setIdeas(next);setCategories(nextCategories);setAssignments(nextAssignments);setLoading(false);const ready=next.filter(x=>!["pending","processing","merged"].includes(x.enrichment?.processing_status??""));const assigned=new Set(nextAssignments.map(x=>x.idea_id));const uncategorized=ready.filter(x=>!assigned.has(x.id));const needsCurator=ready.length>0&&(uncategorized.length>0||nextCategories.length===0);if(needsCurator&&!curatorStarted.current){curatorStarted.current=true;setOrganizing(true);const session=(await s.auth.getSession()).data.session;if(session?.access_token){void fetch("/api/categories/backfill",{method:"POST",headers:{Authorization:`Bearer ${session.access_token}`}}).then(async r=>{if(!r.ok)throw new Error(await r.text());return r.json()}).then(()=>{if(active)timer=setTimeout(load,700)}).catch(e=>{console.error("taxonomy curator request failed",e);if(active)setError("Kipu konnte die Kategorien noch nicht aufbauen.")}).finally(()=>{if(active)setOrganizing(false)})}else setOrganizing(false)}else if(next.some(x=>["pending","processing"].includes(x.enrichment?.processing_status??"")))timer=setTimeout(load,3000)}catch(e){if(active){setError(e instanceof Error?e.message:"Ideen konnten nicht geladen werden.");setLoading(false)}}}void load();return()=>{active=false;if(timer)clearTimeout(timer)}},[]);const categoryById=useMemo(()=>new Map(categories.map(c=>[c.id,c])),[categories]),assignmentByIdea=useMemo(()=>{const m=new Map<string,string[]>();for(const a of assignments){const l=m.get(a.idea_id)??[];l.push(a.category_id);m.set(a.idea_id,l)}return m},[assignments]),mainCategories=useMemo(()=>categories.filter(c=>!c.parent_id),[categories]);const counts=useMemo(()=>{const m=new Map<string,number>();for(const cat of mainCategories){let n=0;for(const idea of ideas)if((assignmentByIdea.get(idea.id)??[]).some(id=>id===cat.id||categoryById.get(id)?.parent_id===cat.id))n++;m.set(cat.id,n)}return m},[ideas,mainCategories,assignmentByIdea,categoryById]);const visibleIdeas=useMemo(()=>{const filtered=selected==="all"?[...ideas]:ideas.filter(i=>(assignmentByIdea.get(i.id)??[]).some(id=>id===selected||categoryById.get(id)?.parent_id===selected));filtered.sort((a,b)=>sort==="oldest"?+new Date(a.created_at)-+new Date(b.created_at):sort==="az"?a.title.localeCompare(b.title,"de",{sensitivity:"base"}):+new Date(b.created_at)-+new Date(a.created_at));return filtered},[ideas,selected,sort,assignmentByIdea,categoryById]);function displayCategory(id:string){const ids=assignmentByIdea.get(id)??[];const chosen=ids.map(x=>categoryById.get(x)).filter(Boolean).sort((a,b)=>Number(Boolean(a?.parent_id))-Number(Boolean(b?.parent_id))).at(-1);return chosen?.name??null}async function deleteAllIdeas(){if(!ideas.length||deleting||!confirm(`Wirklich alle ${ideas.length} Ideen löschen?`))return;setDeleting(true);try{await ensureAnonymousSession();const s=getSupabaseBrowserClient(),u=(await s.auth.getUser()).data.user;if(!u)throw new Error("Keine Session");const{error}=await s.from("ideas").delete().eq("user_id",u.id);if(error)throw error;setIdeas([]);setAssignments([])}catch(e){setError(e instanceof Error?e.message:"Löschen fehlgeschlagen")}finally{setDeleting(false)}}return <main className="mx-auto flex min-h-screen w-full max-w-[430px] flex-col bg-[#fbfaf7] text-black"><header className="flex items-center justify-between px-5 pb-3 pt-7"><div><h1 className="text-[22px] font-semibold">Meine Ideen</h1>{organizing&&<p className="mt-1 flex items-center gap-1.5 text-[11px] text-[#696fc6]"><Sparkles className="h-3 w-3 animate-pulse"/>Kipu baut deine Kategorien auf…</p>}</div><div className="flex items-center gap-4">{ideas.length>0&&<button onClick={deleteAllIdeas} className="text-[#c63f35]">{deleting?<LoaderCircle className="h-5 w-5 animate-spin"/>:<Trash2 className="h-5 w-5"/>}</button>}<button onClick={()=>setFilterOpen(true)} aria-label="Filtern und sortieren"><Filter className="h-5 w-5"/></button><Link href="/search"><Search className="h-5 w-5"/></Link></div></header><div className="px-4 pb-4"><label className="relative block"><select value={selected} onChange={e=>setSelected(e.target.value)} className="w-full appearance-none rounded-[14px] border border-black/5 bg-[#f0efeb] px-4 py-3 pr-10 text-[13px] font-medium outline-none"><option value="all">Alle Kategorien · {ideas.length}</option>{mainCategories.map(c=><option key={c.id} value={c.id}>{c.name} · {counts.get(c.id)??0}</option>)}</select><ChevronDown className="pointer-events-none absolute right-3 top-1/2 h-4 w-4 -translate-y-1/2 text-black/55"/></label></div>{mainCategories.length===0&&!loading&&<div className="mx-4 mb-3 rounded-xl bg-[#f2f0ff] px-4 py-3 text-[11px] text-[#6267b7]">{organizing?"Kipu gruppiert deine Erinnerungen gerade…":"Noch keine Kategorien verfügbar. Kipu baut sie beim nächsten Organisationslauf auf."}</div>}<section className="border-t border-black/5 px-4 pt-4">{loading&&<div className="flex justify-center py-16"><LoaderCircle className="animate-spin"/></div>}{error&&<div className="mb-3 rounded-xl bg-[#fff0ef] p-4 text-xs">{error}</div>}{!loading&&!error&&ideas.length===0&&<div className="py-16 text-center text-sm">Noch keine Ideen gespeichert.</div>}{!loading&&visibleIdeas.length>0&&<div className="space-y-2">{visibleIdeas.map(idea=>{const legacy=idea.enrichment?.category,image=idea.enrichment?.image_url,fit=idea.enrichment?.image_fit??"cover",status=idea.enrichment?.processing_status,processing=status==="pending"||status==="processing",failed=status==="failed",cat=displayCategory(idea.id);return <Link href={`/ideas/${idea.id}`} key={idea.id} className="flex gap-3 rounded-[14px] bg-white p-2 shadow-[0_2px_10px_rgba(0,0,0,.06)]"><div className="flex h-[82px] w-[88px] shrink-0 items-center justify-center overflow-hidden rounded-[10px] bg-[#e8ece4] text-[30px]">{image?<img src={image} alt="" className={`h-full w-full ${fit==="contain"?"object-contain p-1":"object-cover"}`}/>:processing?<Sparkles className="h-7 w-7 animate-pulse text-[#7378db]"/>:emojiFor(legacy)}</div><div className="min-w-0 flex-1 py-1"><h2 className="line-clamp-2 text-[15px] font-semibold">{idea.title}</h2>{processing?<p className="mt-1 flex items-center gap-1 text-xs text-[#696fc6]"><LoaderCircle className="h-3 w-3 animate-spin"/>Kipu ergänzt Details…</p>:failed?<p className="mt-1 text-xs text-[#a33b34]">Aufbereitung offen</p>:<p className="mt-1 line-clamp-1 text-xs text-black/55">{idea.location_label||idea.summary||"Gespeicherte Idee"}</p>}<p className="mt-1 text-[11px] text-black/40">{new Date(idea.created_at).toLocaleDateString("de-CH")}</p>{!processing&&cat&&<div className="mt-1 text-right"><span className="rounded-full bg-[#f0efeb] px-2 py-1 text-[10px]">{cat}</span></div>}</div></Link>})}</div>}</section>{filterOpen&&<div className="fixed inset-0 z-50 flex items-end justify-center bg-black/25" onClick={()=>setFilterOpen(false)}><div className="w-full max-w-[430px] rounded-t-[26px] bg-white p-5 pb-8 shadow-2xl" onClick={e=>e.stopPropagation()}><div className="mb-5 flex items-center justify-between"><h2 className="text-lg font-semibold">Filtern & sortieren</h2><button onClick={()=>setFilterOpen(false)}><X className="h-5 w-5"/></button></div><p className="mb-2 text-[11px] font-semibold uppercase tracking-wide text-black/40">Kategorie</p><div className="mb-6 flex flex-wrap gap-2"><button onClick={()=>setSelected("all")} className={`rounded-full px-3 py-2 text-xs ${selected==="all"?"bg-black text-white":"bg-[#f1f0ed]"}`}>Alle</button>{mainCategories.map(c=><button key={c.id} onClick={()=>setSelected(c.id)} className={`rounded-full px-3 py-2 text-xs ${selected===c.id?"bg-black text-white":"bg-[#f1f0ed]"}`}>{c.name} · {counts.get(c.id)??0}</button>)}</div><p className="mb-2 text-[11px] font-semibold uppercase tracking-wide text-black/40">Sortierung</p><div className="space-y-1">{([{id:"newest",label:"Neueste zuerst",icon:CalendarArrowDown},{id:"oldest",label:"Älteste zuerst",icon:CalendarArrowUp},{id:"az",label:"Titel A–Z",icon:ArrowDownAZ}] as const).map(o=>{const Icon=o.icon;return <button key={o.id} onClick={()=>setSort(o.id)} className="flex w-full items-center justify-between rounded-xl px-3 py-3 text-sm hover:bg-black/5"><span className="flex items-center gap-3"><Icon className="h-4 w-4"/>{o.label}</span>{sort===o.id&&<Check className="h-4 w-4"/>}</button>})}</div><button onClick={()=>setFilterOpen(false)} className="mt-5 w-full rounded-full bg-black py-3 text-sm font-semibold text-white">Anwenden</button></div></div>}<nav className="mt-auto flex items-center justify-around border-t bg-white px-14 py-4"><Link href="/" className="rounded-full px-6 py-3 shadow"><Home className="h-5 w-5 fill-black"/></Link><UserRound className="h-5 w-5"/></nav></main>}
