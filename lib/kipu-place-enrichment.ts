import type { SupabaseClient } from "@supabase/supabase-js";
import { searchPlaces } from "@/lib/kipu-agent-tools";
import { deterministicIdentityMatch } from "@/lib/kipu-duplicate-identity";
const schema={type:"object",additionalProperties:false,properties:{is_physical_place:{type:"boolean"},canonical_name:{anyOf:[{type:"string"},{type:"null"}]},street_address:{anyOf:[{type:"string"},{type:"null"}]},locality:{anyOf:[{type:"string"},{type:"null"}]},review_rating:{anyOf:[{type:"string"},{type:"null"}]},review_count:{anyOf:[{type:"string"},{type:"null"}]},review_summary:{anyOf:[{type:"string"},{type:"null"}]},official_url:{anyOf:[{type:"string"},{type:"null"}]},reviews_url:{anyOf:[{type:"string"},{type:"null"}]}},required:["is_physical_place","canonical_name","street_address","locality","review_rating","review_count","review_summary","official_url","reviews_url"]};
function textOf(p:any){if(typeof p.output_text==="string")return p.output_text;for(const o of p.output??[])for(const c of o.content??[])if(typeof c?.text==="string")return c.text;return null}
function validHttp(v:any){try{const u=new URL(String(v));return /^https?:$/.test(u.protocol)?u.toString():null}catch{return null}}
function mergeLinks(a:any[]=[],b:any[]=[]){const out:any[]=[],seen=new Set<string>();for(const x of [...a,...b]){const u=validHttp(x?.url);if(u&&!seen.has(u)){seen.add(u);out.push({...x,url:u})}}return out.slice(0,4)}
function mergeFacts(a:any[]=[],b:any[]=[]){const out:any[]=[],seen=new Set<string>();for(const x of [...a,...b]){const k=String(x?.label??"").trim().toLowerCase();if(k&&!seen.has(k)&&x?.value){seen.add(k);out.push(x)}}return out.slice(0,7)}
async function geocodeCandidate(queries:string[],latitude?:number|null,longitude?:number|null){for(const q of queries.map(x=>x.trim()).filter(Boolean)){const found:any=await searchPlaces(q,Number.isFinite(latitude)?Number(latitude):undefined,Number.isFinite(longitude)?Number(longitude):undefined),results=Array.isArray(found?.results)?found.results:[],hit=results.find((x:any)=>Number.isFinite(x?.latitude)&&Number.isFinite(x?.longitude));if(hit)return{hit,query:q}}return null}
function identityLinks(links:any[]){return links.filter(x=>{try{const u=new URL(String(x?.url??""));return !/^(?:www\.)?google\.[^/]+$/i.test(u.hostname)&&!/(?:^|\.)google\.com$/i.test(u.hostname)&&!/(?:^|\.)google\.ch$/i.test(u.hostname)&&!/(?:^|\.)openstreetmap\.org$/i.test(u.hostname)}catch{return false}})}
async function deterministicPlaceDuplicate(s:SupabaseClient,userId:string,ideaId:string,label:string,lat:number,lon:number,links:any[]){const{data,error}=await s.from("ideas").select("id,title,summary,original_input,tags,people,location_label,enrichment,created_at").eq("user_id",userId).neq("id",ideaId).order("created_at",{ascending:false}).limit(120);if(error)return null;const current:any={id:ideaId,title:label,location_label:label,latitude:lat,longitude:lon,urls:identityLinks(links).map(x=>x.url)};let best:any=null;for(const row of data??[]){const match=deterministicIdentityMatch(current,row);if(match.confidence==="none")continue;if(!best||match.score>best.match.score)best={row,match}}return best}
export async function ensurePlaceEnrichment(s:SupabaseClient,userId:string,openAiKey:string,ideaId:string){const{data:idea,error}=await s.from("ideas").select("id,title,summary,original_input,tags,latitude,longitude,location_label,enrichment").eq("id",ideaId).eq("user_id",userId).single();if(error||!idea)throw error??new Error("idea_not_found");const e=(idea.enrichment??{}) as Record<string,any>;if(e.processing_status!=="ready")return{updated:false,reason:"not_ready"};const captureLat=Number.isFinite(idea.latitude)?Number(idea.latitude):Number.isFinite(e.capture_location?.latitude)?Number(e.capture_location.latitude):null,captureLon=Number.isFinite(idea.longitude)?Number(idea.longitude):Number.isFinite(e.capture_location?.longitude)?Number(e.capture_location.longitude):null,inputImage=typeof e.input_image==="string"&&e.input_image.startsWith("data:image/")?e.input_image:null,userContent:any[]=[{type:"input_text",text:JSON.stringify({title:idea.title,summary:idea.summary,original_input:idea.original_input,tags:idea.tags,location_label:idea.location_label,capture_location:Number.isFinite(captureLat)&&Number.isFinite(captureLon)?{latitude:captureLat,longitude:captureLon}:null})}];if(inputImage)userContent.push({type:"input_image",image_url:inputImage,detail:"high"});const r=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{Authorization:`Bearer ${openAiKey}`,"Content-Type":"application/json"},body:JSON.stringify({model:"gpt-5.6-luna",tools:[{type:"web_search"}],include:["web_search_call.action.sources"],input:[{role:"system",content:[{type:"input_text",text:"Du bist Kipus Place-Resolver. Entscheide, ob die Erinnerung eine konkrete öffentlich auffindbare physische Location/POI meint. Nutze Bilder zur Entitätsidentifikation. Aufnahme-GPS ist nur Disambiguierung. Identifiziere bei einem POI kanonischen Namen, möglichst genaue Adresse und Ort. review_rating/review_count nur gestützt, review_summary höchstens ein kurzer Satz. official_url nur offiziell. Wenn keine konkrete physische Entität gemeint ist: is_physical_place=false, Rest null."}]},{role:"user",content:userContent}],text:{format:{type:"json_schema",name:"kipu_place_resolution",strict:true,schema}}})});if(!r.ok)throw new Error(`place_openai_${r.status}`);const raw=textOf(await r.json());if(!raw)throw new Error("place_no_output");const p=JSON.parse(raw);if(!p.is_physical_place||!p.canonical_name){console.info("Kipu place resolver",{ideaId,result:"not_place"});return{updated:false,reason:"not_place"}}const queries=[[p.canonical_name,p.street_address,p.locality].filter(Boolean).join(", "),[p.street_address,p.locality].filter(Boolean).join(", "),[p.canonical_name,p.locality].filter(Boolean).join(", "),String(p.canonical_name??""),String(p.street_address??"")],geo=await geocodeCandidate(queries,captureLat,captureLon);if(!geo){console.info("Kipu place resolver",{ideaId,result:"no_coordinates"});return{updated:false,reason:"no_coordinates"}}const first:any=geo.hit,lat=Number(first.latitude),lon=Number(first.longitude),label=[p.canonical_name,p.locality].filter(Boolean).join(", ")||first.displayName,maps=`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent([p.canonical_name,p.street_address,p.locality].filter(Boolean).join(" "))}`,facts:any[]=[];if(p.street_address)facts.push({label:"Adresse",value:String(p.street_address)});if(p.review_rating)facts.push({label:"Bewertung",value:p.review_count?`${p.review_rating} (${p.review_count})`:p.review_rating});if(p.review_summary)facts.push({label:"Rezensionen",value:p.review_summary});if(first.openingHours)facts.push({label:"Öffnungszeiten",value:String(first.openingHours)});if(first.phone)facts.push({label:"Telefon",value:String(first.phone)});const links:any[]=[{label:"Google Maps",url:maps}],official=validHttp(p.official_url)||validHttp(first.website);if(official)links.push({label:"Website",url:official});const reviews=validHttp(p.reviews_url);if(reviews)links.push({label:"Rezensionen",url:reviews});const duplicate=await deterministicPlaceDuplicate(s,userId,ideaId,label,lat,lon,links),priorConfidence=e.duplicate_confidence??"none",takePlace=duplicate?.match?.confidence==="high"||priorConfidence==="none",duplicateId=takePlace?(duplicate?.row?.id??e.duplicate_of_idea_id??null):(e.duplicate_of_idea_id??null),duplicateConfidence=takePlace?(duplicate?.match?.confidence??priorConfidence):priorConfidence,enrichment={...e,subject_coordinates:{latitude:lat,longitude:lon},facts:mergeFacts(facts,e.facts??[]),useful_links:mergeLinks(links,e.useful_links??[]),duplicate_of_idea_id:duplicateId,duplicate_confidence:duplicateConfidence,duplicate_reason:takePlace&&duplicate?duplicate.match.reasons.join("; "):e.duplicate_reason??null,duplicate_matchers:takePlace&&duplicate?duplicate.match.matchers:e.duplicate_matchers??[],duplicate_decision_source:takePlace&&duplicate?"deterministic_place_identity":e.duplicate_decision_source??null,possible_duplicate_of:duplicateConfidence==="possible"?duplicateId:null,place_enriched_at:new Date().toISOString(),place_enrichment_version:5,place_geocode_query:geo.query};const u=await s.from("ideas").update({location_label:label,location_source:"researched",enrichment}).eq("id",ideaId).eq("user_id",userId);if(u.error)throw u.error;console.info("Kipu place resolver",{ideaId,result:"updated",duplicateOf:duplicateId,duplicateConfidence,matchers:duplicate?.match?.matchers??[]});return{updated:true,latitude:lat,longitude:lon,label,duplicate_of_idea_id:duplicateId,duplicate_confidence:duplicateConfidence}}
