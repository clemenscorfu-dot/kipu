import { createClient, type SupabaseClient } from "@supabase/supabase-js";
import { findSimilarIdeas, kipuFunctionTools, runKipuFunctionTool } from "@/lib/kipu-agent-tools";

const factSchema={type:"object",additionalProperties:false,properties:{label:{type:"string"},value:{type:"string"}},required:["label","value"]};
const linkSchema={type:"object",additionalProperties:false,properties:{label:{type:"string"},url:{type:"string"}},required:["label","url"]};
const memorySchema={type:"object",additionalProperties:false,properties:{title:{type:"string"},summary:{type:"string",description:"Kurze nutzerorientierte Zusammenfassung. Bei inhaltstragenden Werken oder Medien erklärt sie primär, worum es inhaltlich geht; technische Metadaten gehören in facts."},tags:{type:"array",items:{type:"string"},maxItems:3},people:{type:"array",items:{type:"string"},maxItems:8},category:{type:"string"},assigned_category_id:{anyOf:[{type:"string"},{type:"null"}]},duplicate_of_idea_id:{anyOf:[{type:"string"},{type:"null"}]},duplicate_confidence:{type:"string",enum:["none","possible","high"]},subject_location:{anyOf:[{type:"string"},{type:"null"}]},subject_latitude:{anyOf:[{type:"number"},{type:"null"}]},subject_longitude:{anyOf:[{type:"number"},{type:"null"}]},subject_location_is_from_user:{type:"boolean"},facts:{type:"array",items:factSchema,maxItems:5},useful_links:{type:"array",items:linkSchema,maxItems:3},representative_image_url:{anyOf:[{type:"string"},{type:"null"}]},use_input_image:{type:"boolean"},representative_image_fit:{type:"string",enum:["cover","contain"]},image_reason:{type:"string"},related_idea_ids:{type:"array",items:{type:"string"},maxItems:5}},required:["title","summary","tags","people","category","assigned_category_id","duplicate_of_idea_id","duplicate_confidence","subject_location","subject_latitude","subject_longitude","subject_location_is_from_user","facts","useful_links","representative_image_url","use_input_image","representative_image_fit","image_reason","related_idea_ids"]};
type Source={title?:string;url:string};type AgentResponse={id?:string;output_text?:string;output?:Array<Record<string,any>>};export type EnrichIdeaPayload={ideaId:string;userId:string;accessToken:string};
function extractResponseText(p:AgentResponse){if(typeof p.output_text==="string"&&p.output_text.trim())return p.output_text;for(const o of p.output??[])for(const c of o.content??[])if(typeof c?.text==="string"&&c.text.trim())return c.text;return null}
function extractSources(p:AgentResponse){const out:Source[]=[];for(const o of p.output??[]){if(o.type!=="web_search_call")continue;for(const s of o.action?.sources??[])if(s?.url)out.push({title:s.title,url:s.url})}return out}
function cleanGeneratedText(v:string){return v.replace(/\[([^\]]+)\]\(https?:\/\/[^)]+\)/g,"$1").replace(/\((?:https?:\/\/[^)]+)\)/g,"").replace(/https?:\/\/\S+/g,"").replace(/\s{2,}/g," ").trim()}
function userLinksCaptureToSubject(text:string){const v=text.toLocaleLowerCase("de-CH").replace(/\s+/g," ").trim();return Boolean(v)&&(/\b(hier|genau hier|an diesem ort|an dieser stelle|diese stelle|dieser ort|da wo ich bin|dort wo ich bin|mein aktueller standort)\b/i.test(v)||/\b(dies(?:e|er|es)\s+[^.!?]{0,35}\s+hier)\b/i.test(v))}
async function callOpenAI(key:string,body:Record<string,unknown>){const r=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{Authorization:`Bearer ${key}`,"Content-Type":"application/json"},body:JSON.stringify(body)});if(!r.ok)throw new Error(`openai_${r.status}`);return await r.json() as AgentResponse}
async function validateImage(url:string|null){if(!url)return null;let parsed:URL;try{parsed=new URL(url)}catch{return null}if(!/^https?:$/.test(parsed.protocol))return null;try{const r=await fetch(url,{redirect:"follow",headers:{"User-Agent":"Mozilla/5.0 (Linux; Android 16) AppleWebKit/537.36 Chrome/140 Mobile Safari/537.36",Accept:"image/avif,image/webp,image/apng,image/svg+xml,image/*,*/*;q=0.8"},signal:AbortSignal.timeout(6000)}),type=(r.headers.get("content-type")||"").toLowerCase();if(r.ok&&type.startsWith("image/"))return r.url||url;if(r.ok&&type&&!type.includes("text/html")&&!type.includes("application/json"))return r.url||url;if([401,403,429].includes(r.status))return url;return null}catch{return url}}
function identifiersFromFacts(facts:any[]=[]){const out:Array<{type:string;value:string}>=[];for(const f of facts){const label=String(f?.label??"").toLowerCase(),value=String(f?.value??"").trim();if(!value)continue;if(label.includes("isbn"))out.push({type:"isbn",value});else if(/ean|gtin|barcode/.test(label))out.push({type:"gtin",value});else if(/(^|\s)id$|produkt.?id|product.?id|entity.?id|imdb|tmdb|spotify.?id|osm.?id|place.?id/.test(label))out.push({type:label||"external_id",value})}return out}
function urlsFrom(text:string){return text.match(/https?:\/\/[^\s<>"')\]]+/gi)??[]}

async function runAgent(key:string,supabase:SupabaseClient,userId:string,currentIdeaId:string,text:string,imageDataUrl:string|null,latitude?:number|null,longitude?:number|null){const tools=[{type:"web_search"},...kipuFunctionTools],allSources:Source[]=[];const hasCoords=Number.isFinite(latitude)&&Number.isFinite(longitude),hasImage=Boolean(imageDataUrl?.startsWith("data:image/"));const system=`Du bist Kipu, ein persönlicher Memory-Agent. Nutze Tools selbstständig; keine Kategorie-Regelbäume.
Quellen: Vom Nutzer gelieferte URLs, Bilder und Dokumente sind Wahrnehmungskanäle. Relevante URLs untersuchen; aus unverstandenen Quellen nichts erfinden.
Kontext-Isolation: Bestehende Erinnerungen nur für Doubletten, Disambiguierung und echte Beziehungen nutzen; keine fremden Sachinhalte übernehmen.
Doubletten: Rufe bei JEDEM Capture find_similar_ideas auf. Kernfrage: "Meint diese neue Erinnerung dieselbe konkrete Sache wie eine bestehende Erinnerung – unabhängig von Formulierung, persönlichem Kontext oder Eingabekanal?" Trenne Entity Identity (WAS ist gespeichert?) von persönlicher Absicht (WARUM wird es gespeichert?). "Mit Lydia dort essen", "später kaufen", "für die Kinder" oder "mal anschauen" verändern die Identität der Sache nicht. Nutze harte IDs, URLs, kanonischen Namen, Fakten, Orte und erkannte Bild-Entität. currentIdeaId=${currentIdeaId} muss ausgeschlossen bleiben. high nur bei sehr sicher derselben konkreten Entität; gleiche Kategorie, Autor, Marke oder ähnliche Variante reicht nicht. Zwei Filialen einer Kette sind nicht automatisch dieselbe Entität. Doubletten nur markieren, niemals löschen oder zusammenführen.
Kategorien: get_categories aufrufen, bestehende Kategorien bevorzugen, max. 2 Ebenen; manage_categories hier nur create.
Bilder: Für öffentlich identifizierbare Entitäten mit visuellem Wiedererkennungswert search_images nutzen. Input-Foto bevorzugen, wenn es die Sache besser repräsentiert.
Summary: Bei Büchern, Hörbüchern, Podcasts, Filmen, Artikeln, Videos und vergleichbaren Werken in 1–3 kurzen Sätzen sagen, worum es inhaltlich geht; technische Angaben in facts.
Qualität: Vision direkt nutzen, öffentliche Entitäten proaktiv recherchieren, max. 3 Links, 5 Fakten, 3 Tags, nichts erfinden. Capture Location != Subject Location. Finale Antwort nur im Schema.`;const userText=text.trim()||"Der Nutzer möchte sich dieses Foto merken.",ctx=hasCoords?`Nutzereingabe:\n${userText}\n\nAufnahmekontext GPS ${latitude}, ${longitude}; nicht automatisch Subject Location.`:`Nutzereingabe:\n${userText}\n\nKein GPS-Aufnahmekontext.`,content:any[]=[{type:"input_text",text:ctx}];if(hasImage)content.push({type:"input_image",image_url:imageDataUrl,detail:"high"});let response=await callOpenAI(key,{model:"gpt-5.6-luna",tools,include:["web_search_call.action.sources"],input:[{role:"system",content:[{type:"input_text",text:system}]},{role:"user",content}],text:{format:{type:"json_schema",name:"kipu_memory",strict:true,schema:memorySchema}}});for(let step=0;step<12;step++){allSources.push(...extractSources(response));const calls=(response.output??[]).filter(i=>i.type==="function_call");if(!calls.length)break;const outputs=[];for(const call of calls){let args={};try{args=JSON.parse(call.arguments??"{}")}catch{}const result=await runKipuFunctionTool(call.name,args,{supabase,userId,openAiKey:key,currentIdeaId});outputs.push({type:"function_call_output",call_id:call.call_id,output:JSON.stringify(result)})}response=await callOpenAI(key,{model:"gpt-5.6-luna",previous_response_id:response.id,tools,include:["web_search_call.action.sources"],input:outputs,text:{format:{type:"json_schema",name:"kipu_memory",strict:true,schema:memorySchema}}})}allSources.push(...extractSources(response));const raw=extractResponseText(response);if(!raw)throw new Error("agent_no_final_output");const memory=JSON.parse(raw) as any;memory.summary=cleanGeneratedText(memory.summary);memory.facts=(memory.facts??[]).map((f:any)=>({label:cleanGeneratedText(f.label),value:cleanGeneratedText(f.value)}));memory.useful_links=(memory.useful_links??[]).filter((l:any)=>{try{return Boolean(l.label)&&new URL(l.url).protocol.startsWith("http")}catch{return false}});const explicitLocation=hasCoords&&userLinksCaptureToSubject(text);memory.subject_location_is_from_user=Boolean(explicitLocation);const subjectCoords=Number.isFinite(memory.subject_latitude)&&Number.isFinite(memory.subject_longitude);if(memory.subject_location&&!explicitLocation&&!subjectCoords){memory.subject_location=null;memory.subject_latitude=null;memory.subject_longitude=null;memory.useful_links=memory.useful_links.filter((l:any)=>!String(l.url).includes("google.com/maps"))}memory.representative_image_url=await validateImage(memory.representative_image_url??null);if(!hasImage)memory.use_input_image=false;if(memory.assigned_category_id){const{data}=await supabase.from("categories").select("id").eq("id",memory.assigned_category_id).eq("user_id",userId).maybeSingle();if(!data)memory.assigned_category_id=null}if(memory.duplicate_of_idea_id===currentIdeaId){memory.duplicate_of_idea_id=null;memory.duplicate_confidence="none"}const unique:Source[]=[],seen=new Set<string>();for(const s of allSources)if(s.url&&!seen.has(s.url)){seen.add(s.url);unique.push(s)}return{memory,sources:unique.slice(0,8)}}

async function resolveDuplicate(supabase:SupabaseClient,userId:string,key:string,idea:any,memory:any,sources:Source[]){const urls=[...urlsFrom(String(idea.original_input??"")),...(memory.useful_links??[]).map((x:any)=>x.url),...sources.map(x=>x.url)].filter(Boolean);const result:any=await findSimilarIdeas({text:String(idea.original_input??memory.title??""),current_idea_id:idea.id,canonical_name:memory.title,summary:memory.summary,tags:memory.tags??[],people:memory.people??[],location_label:memory.subject_location,urls,identifiers:identifiersFromFacts(memory.facts),latitude:memory.subject_latitude,longitude:memory.subject_longitude},{supabase,userId,openAiKey:key,currentIdeaId:idea.id});const best=result?.best;if(best?.idea_id&&best.idea_id!==idea.id&&(best.confidence==="high"||best.confidence==="possible"))return{ideaId:best.idea_id,confidence:best.confidence,reason:best.reason??null,matchers:best.matchers??[],source:best.decision_source??"identity_pipeline"};if(memory.duplicate_of_idea_id&&memory.duplicate_of_idea_id!==idea.id&&memory.duplicate_confidence!=="none")return{ideaId:memory.duplicate_of_idea_id,confidence:memory.duplicate_confidence,reason:"agent identity assessment",matchers:["agent_identity"],source:"agent"};return{ideaId:null,confidence:"none",reason:null,matchers:[],source:"identity_pipeline"}}

export async function enrichPendingIdea(payload:EnrichIdeaPayload){const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,openAiKey=process.env.OPENAI_API_KEY;if(!url||!key||!openAiKey)throw new Error("missing_server_configuration");const supabase=createClient(url,key,{global:{headers:{Authorization:`Bearer ${payload.accessToken}`}},auth:{persistSession:false,autoRefreshToken:false}}),{data:idea,error}=await supabase.from("ideas").select("*").eq("id",payload.ideaId).eq("user_id",payload.userId).single();if(error||!idea)throw new Error(error?.message||"idea_not_found");const existing=(idea.enrichment??{}) as Record<string,any>,inputImage=typeof existing.input_image==="string"?existing.input_image:null;try{await supabase.from("ideas").update({enrichment:{...existing,processing_status:"processing",processing_started_at:new Date().toISOString()}}).eq("id",idea.id).eq("user_id",payload.userId);const{memory,sources}=await runAgent(openAiKey,supabase,payload.userId,idea.id,idea.original_input??"",inputImage,idea.latitude,idea.longitude);const duplicate=await resolveDuplicate(supabase,payload.userId,openAiKey,idea,memory,sources),hero=memory.use_input_image&&inputImage?inputImage:memory.representative_image_url,now=new Date().toISOString();console.info("Kipu duplicate final",{ideaId:idea.id,duplicateOf:duplicate.ideaId,confidence:duplicate.confidence,matchers:duplicate.matchers,source:duplicate.source});const enrichment={...existing,category:memory.category,facts:memory.facts,sources,useful_links:memory.useful_links,subject_coordinates:memory.subject_latitude!=null&&memory.subject_longitude!=null?{latitude:memory.subject_latitude,longitude:memory.subject_longitude}:null,image_url:hero,image_fit:memory.representative_image_fit,image_reason:memory.image_reason,input_image:inputImage,input_image_used:Boolean(memory.use_input_image&&inputImage),related_idea_ids:memory.related_idea_ids,duplicate_of_idea_id:duplicate.ideaId,duplicate_confidence:duplicate.confidence,duplicate_reason:duplicate.reason,duplicate_matchers:duplicate.matchers,duplicate_decision_source:duplicate.source,possible_duplicate_of:duplicate.confidence==="possible"?duplicate.ideaId:null,agentic:true,toolbox_version:14,model:"gpt-5.6-luna",processing_status:"ready",enriched_at:now};const update=await supabase.from("ideas").update({title:memory.title,summary:memory.summary,tags:memory.tags,people:memory.people,location_label:memory.subject_location,location_source:memory.subject_location?(memory.subject_location_is_from_user?"extracted":"researched"):idea.latitude!=null?"device":null,enrichment}).eq("id",idea.id).eq("user_id",payload.userId);if(update.error)throw update.error;if(memory.assigned_category_id){try{await supabase.from("idea_categories").delete().eq("idea_id",idea.id);await supabase.from("idea_categories").insert({idea_id:idea.id,category_id:memory.assigned_category_id})}catch(categoryError){console.error("Idea category assignment failed",categoryError)}}return{ideaId:idea.id,status:"ready",duplicateOf:duplicate.ideaId,duplicateConfidence:duplicate.confidence}}catch(error){const message=error instanceof Error?error.message:"enrichment_failed";await supabase.from("ideas").update({enrichment:{...existing,processing_status:"failed",processing_error:message,processing_failed_at:new Date().toISOString()}}).eq("id",idea.id).eq("user_id",payload.userId);throw error}}
