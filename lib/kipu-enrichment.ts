import { createClient, type SupabaseClient } from "@supabase/supabase-js";
import { kipuFunctionTools, runKipuFunctionTool } from "@/lib/kipu-agent-tools";

const factSchema={type:"object",additionalProperties:false,properties:{label:{type:"string"},value:{type:"string"}},required:["label","value"]};
const linkSchema={type:"object",additionalProperties:false,properties:{label:{type:"string"},url:{type:"string"}},required:["label","url"]};
const memorySchema={type:"object",additionalProperties:false,properties:{title:{type:"string"},summary:{type:"string"},tags:{type:"array",items:{type:"string"},maxItems:3},people:{type:"array",items:{type:"string"},maxItems:8},category:{type:"string"},assigned_category_id:{anyOf:[{type:"string"},{type:"null"}]},duplicate_of_idea_id:{anyOf:[{type:"string"},{type:"null"}]},duplicate_confidence:{type:"string",enum:["none","possible","high"]},subject_location:{anyOf:[{type:"string"},{type:"null"}]},subject_latitude:{anyOf:[{type:"number"},{type:"null"}]},subject_longitude:{anyOf:[{type:"number"},{type:"null"}]},subject_location_is_from_user:{type:"boolean"},facts:{type:"array",items:factSchema,maxItems:5},useful_links:{type:"array",items:linkSchema,maxItems:3},representative_image_url:{anyOf:[{type:"string"},{type:"null"}]},use_input_image:{type:"boolean"},representative_image_fit:{type:"string",enum:["cover","contain"]},image_reason:{type:"string"},related_idea_ids:{type:"array",items:{type:"string"},maxItems:5}},required:["title","summary","tags","people","category","assigned_category_id","duplicate_of_idea_id","duplicate_confidence","subject_location","subject_latitude","subject_longitude","subject_location_is_from_user","facts","useful_links","representative_image_url","use_input_image","representative_image_fit","image_reason","related_idea_ids"]};
type Source={title?:string;url:string};type AgentResponse={id?:string;output_text?:string;output?:Array<Record<string,any>>};export type EnrichIdeaPayload={ideaId:string;userId:string;accessToken:string};
function extractResponseText(payload:AgentResponse){if(typeof payload.output_text==="string"&&payload.output_text.trim())return payload.output_text;for(const output of payload.output??[])for(const content of output.content??[])if(typeof content?.text==="string"&&content.text.trim())return content.text;return null}function extractSources(payload:AgentResponse){const sources:Source[]=[];for(const output of payload.output??[]){if(output.type!=="web_search_call")continue;for(const source of output.action?.sources??[])if(source?.url)sources.push({title:source.title,url:source.url})}return sources}function cleanGeneratedText(value:string){return value.replace(/\[([^\]]+)\]\(https?:\/\/[^)]+\)/g,"$1").replace(/\((?:https?:\/\/[^)]+)\)/g,"").replace(/https?:\/\/\S+/g,"").replace(/\s{2,}/g," ").trim()}function userLinksCaptureToSubject(text:string){const value=text.toLocaleLowerCase("de-CH").replace(/\s+/g," ").trim();if(!value)return false;return /\b(hier|genau hier|an diesem ort|an dieser stelle|diese stelle|dieser ort|da wo ich bin|dort wo ich bin|mein aktueller standort)\b/i.test(value)||/\b(dies(?:e|er|es)\s+[^.!?]{0,35}\s+hier)\b/i.test(value)}async function callOpenAI(openAiKey:string,body:Record<string,unknown>){const response=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{Authorization:`Bearer ${openAiKey}`,"Content-Type":"application/json"},body:JSON.stringify(body)});if(!response.ok){const detail=await response.text();console.error("Kipu agent OpenAI error",response.status,detail);throw new Error(`openai_${response.status}`)}return(await response.json()) as AgentResponse}async function validateImage(url:string|null){if(!url)return null;try{const response=await fetch(url,{method:"GET",redirect:"follow",headers:{"User-Agent":"Mozilla/5.0 KipuBot/0.7","Accept":"image/*"},signal:AbortSignal.timeout(5000)});const type=response.headers.get("content-type")||"";if(!response.ok||!type.startsWith("image/"))return null;return response.url||url}catch{return null}}
async function runAgent(openAiKey:string,supabase:SupabaseClient,userId:string,currentIdeaId:string,text:string,imageDataUrl:string|null,latitude?:number|null,longitude?:number|null){const tools=[{type:"web_search"},...kipuFunctionTools];const allSources:Source[]=[];const hasCoords=Number.isFinite(latitude)&&Number.isFinite(longitude);const hasImage=Boolean(imageDataUrl?.startsWith("data:image/"));const system=`Du bist Kipu, ein persönlicher Memory-Agent. Aus knappem Text, Sprache oder Foto machst du eine nützliche Erinnerung. Nutze Tools selbstständig; keine Kategorie-Regelbäume.

Toolbox: Web Search, search_images, inspect_web_page, search_places, reverse_geocode, search_my_ideas, find_similar_ideas, get_categories, manage_categories.

Doubletten sind zentral:
- Rufe bei JEDEM Capture find_similar_ideas auf. Prüfe semantisch, ob dieselbe reale Entität/Sache bereits gespeichert ist. Identische Formulierung ist NICHT nötig.
- currentIdeaId=${currentIdeaId} ist der gerade neu erzeugte Rohdatensatz und darf niemals als duplicate_of_idea_id gewählt werden.
- high nur wenn du sehr sicher bist, dass es dieselbe konkrete Entität ist (z.B. dasselbe Buch+Autor, dasselbe Restaurant/POI, dasselbe konkrete Produkt/Modell). Ähnliche Themen, verschiedene Ausgaben/Varianten oder nur ähnliche Namen sind nicht high.
- Bei high: duplicate_of_idea_id = bestehende ältere Idee. Bei Unsicherheit: duplicate_confidence=possible, duplicate_of_idea_id darf Kandidat sein. Sonst none/null.
- Bei high wird die App beide Erinnerungen zusammenführen; erzeuge deshalb trotzdem die bestmögliche aktuelle Erinnerung.

Kategorien: Rufe get_categories auf. Grobe stabile Navigation, Tags feine Details. Ziel etwa 5–12 Hauptkategorien, max. 2 Ebenen. Bestehende Kategorien bevorzugen; manage_categories im Capture nur create. assigned_category_id muss real sein oder null.

Qualität: Vision direkt nutzen. Öffentlich identifizierbare Entitäten proaktiv recherchieren. Höchstens 3 nützliche Links, 5 Fakten, 3 Tags. Nichts erfinden. Capture Location != Subject Location. Ein recherchierter Subject-Ort braucht Subject-Evidenz. Finale Antwort nur im Schema.`;const userText=text.trim()||"Der Nutzer möchte sich dieses Foto merken.";const contextText=hasCoords?`Nutzereingabe:\n${userText}\n\nAufnahmekontext GPS ${latitude}, ${longitude}; nicht automatisch Subject Location.`:`Nutzereingabe:\n${userText}\n\nKein GPS-Aufnahmekontext.`;const content:any[]=[{type:"input_text",text:contextText}];if(hasImage)content.push({type:"input_image",image_url:imageDataUrl,detail:"high"});let response=await callOpenAI(openAiKey,{model:"gpt-5.6-luna",tools,include:["web_search_call.action.sources"],input:[{role:"system",content:[{type:"input_text",text:system}]},{role:"user",content}],text:{format:{type:"json_schema",name:"kipu_memory",strict:true,schema:memorySchema}}});for(let step=0;step<12;step++){allSources.push(...extractSources(response));const calls=(response.output??[]).filter(item=>item.type==="function_call");if(!calls.length)break;const outputs=[];for(const call of calls){let args:Record<string,unknown>={};try{args=JSON.parse(call.arguments??"{}")}catch{}const result=await runKipuFunctionTool(call.name,args,{supabase,userId,openAiKey});outputs.push({type:"function_call_output",call_id:call.call_id,output:JSON.stringify(result)})}response=await callOpenAI(openAiKey,{model:"gpt-5.6-luna",previous_response_id:response.id,tools,include:["web_search_call.action.sources"],input:outputs,text:{format:{type:"json_schema",name:"kipu_memory",strict:true,schema:memorySchema}}});}allSources.push(...extractSources(response));const raw=extractResponseText(response);if(!raw)throw new Error("agent_no_final_output");const memory=JSON.parse(raw) as any;memory.summary=cleanGeneratedText(memory.summary);memory.facts=(memory.facts??[]).map((f:any)=>({label:cleanGeneratedText(f.label),value:cleanGeneratedText(f.value)}));memory.useful_links=(memory.useful_links??[]).filter((l:any)=>{try{return Boolean(l.label)&&new URL(l.url).protocol.startsWith("http")}catch{return false}});const explicitCaptureSubject=hasCoords&&userLinksCaptureToSubject(text);memory.subject_location_is_from_user=Boolean(explicitCaptureSubject);const hasSubjectCoords=Number.isFinite(memory.subject_latitude)&&Number.isFinite(memory.subject_longitude);if(memory.subject_location&&!explicitCaptureSubject&&!hasSubjectCoords){memory.subject_location=null;memory.subject_latitude=null;memory.subject_longitude=null;memory.useful_links=memory.useful_links.filter((l:any)=>!String(l.url).includes("google.com/maps"))}memory.representative_image_url=await validateImage(memory.representative_image_url??null);if(!hasImage)memory.use_input_image=false;if(memory.assigned_category_id){const{data}=await supabase.from("categories").select("id").eq("id",memory.assigned_category_id).eq("user_id",userId).maybeSingle();if(!data)memory.assigned_category_id=null}if(memory.duplicate_of_idea_id===currentIdeaId){memory.duplicate_of_idea_id=null;memory.duplicate_confidence="none"}const uniqueSources:Source[]=[];const seen=new Set<string>();for(const source of allSources){if(!source.url||seen.has(source.url))continue;seen.add(source.url);uniqueSources.push(source)}return{memory,sources:uniqueSources.slice(0,8)}}
function chooseBetterImage(newImage:string|null,oldImage:string|null){return newImage||oldImage||null}function mergeLinks(a:any[]=[],b:any[]=[]){const out:any[]=[];const seen=new Set<string>();for(const x of [...a,...b])if(x?.url&&!seen.has(x.url)){seen.add(x.url);out.push(x)}return out.slice(0,3)}function mergeFacts(a:any[]=[],b:any[]=[]){const out:any[]=[];const seen=new Set<string>();for(const x of [...a,...b]){const key=String(x?.label??"").toLowerCase();if(key&&!seen.has(key)){seen.add(key);out.push(x)}}return out.slice(0,5)}
export async function enrichPendingIdea(payload:EnrichIdeaPayload){const supabaseUrl=process.env.NEXT_PUBLIC_SUPABASE_URL,supabaseKey=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,openAiKey=process.env.OPENAI_API_KEY;if(!supabaseUrl||!supabaseKey||!openAiKey)throw new Error("missing_server_configuration");const supabase=createClient(supabaseUrl,supabaseKey,{global:{headers:{Authorization:`Bearer ${payload.accessToken}`}},auth:{persistSession:false,autoRefreshToken:false}});const{data:idea,error:readError}=await supabase.from("ideas").select("*").eq("id",payload.ideaId).eq("user_id",payload.userId).single();if(readError||!idea)throw new Error(readError?.message||"idea_not_found");const existing=(idea.enrichment??{}) as Record<string,any>;const imageDataUrl=typeof existing.input_image==="string"?existing.input_image:null;try{await supabase.from("ideas").update({enrichment:{...existing,processing_status:"processing",processing_started_at:new Date().toISOString()}}).eq("id",idea.id).eq("user_id",payload.userId);const{memory,sources}=await runAgent(openAiKey,supabase,payload.userId,idea.id,idea.original_input??"",imageDataUrl,idea.latitude,idea.longitude);const heroImage=memory.use_input_image&&imageDataUrl?imageDataUrl:memory.representative_image_url;const now=new Date().toISOString();
let duplicate:any=null;if(memory.duplicate_confidence==="high"&&memory.duplicate_of_idea_id){const{data}=await supabase.from("ideas").select("*").eq("id",memory.duplicate_of_idea_id).eq("user_id",payload.userId).maybeSingle();duplicate=data}
if(duplicate){const old=(duplicate.enrichment??{}) as Record<string,any>;const mergedEnrichment={...old,category:memory.category||old.category,facts:mergeFacts(memory.facts,old.facts),sources:[...sources,...(old.sources??[])].slice(0,8),useful_links:mergeLinks(memory.useful_links,old.useful_links),subject_coordinates:memory.subject_latitude!=null&&memory.subject_longitude!=null?{latitude:memory.subject_latitude,longitude:memory.subject_longitude}:old.subject_coordinates??null,image_url:chooseBetterImage(heroImage,old.image_url??null),image_fit:heroImage?memory.representative_image_fit:(old.image_fit??"cover"),image_reason:memory.image_reason||old.image_reason,input_image:old.input_image??imageDataUrl,input_image_used:Boolean(old.input_image_used||memory.use_input_image),related_idea_ids:Array.from(new Set([...(old.related_idea_ids??[]),...(memory.related_idea_ids??[])] )).filter(id=>id!==idea.id).slice(0,5),agentic:true,toolbox_version:9,model:"gpt-5.6-luna",processing_status:"ready",enriched_at:now,mention_count:Number(old.mention_count??1)+1,last_mentioned_at:now,last_duplicate_input:idea.original_input};const{error:updateDup}=await supabase.from("ideas").update({title:memory.title||duplicate.title,summary:memory.summary||duplicate.summary,tags:Array.from(new Set([...(memory.tags??[]),...(duplicate.tags??[])])).slice(0,3),people:Array.from(new Set([...(memory.people??[]),...(duplicate.people??[])])).slice(0,8),location_label:memory.subject_location||duplicate.location_label,location_source:memory.subject_location?(memory.subject_location_is_from_user?"extracted":"researched"):duplicate.location_source,enrichment:mergedEnrichment}).eq("id",duplicate.id).eq("user_id",payload.userId);if(updateDup)throw updateDup;if(memory.assigned_category_id){await supabase.from("idea_categories").delete().eq("idea_id",duplicate.id);await supabase.from("idea_categories").insert({idea_id:duplicate.id,category_id:memory.assigned_category_id})}const{error:deleteNew}=await supabase.from("ideas").delete().eq("id",idea.id).eq("user_id",payload.userId);if(deleteNew)throw deleteNew;return{ideaId:duplicate.id,mergedFromIdeaId:idea.id,status:"merged"}}
const enrichment={...existing,category:memory.category,facts:memory.facts,sources,useful_links:memory.useful_links,subject_coordinates:memory.subject_latitude!=null&&memory.subject_longitude!=null?{latitude:memory.subject_latitude,longitude:memory.subject_longitude}:null,image_url:heroImage,image_fit:memory.representative_image_fit,image_reason:memory.image_reason,input_image:imageDataUrl,input_image_used:Boolean(memory.use_input_image&&imageDataUrl),related_idea_ids:memory.duplicate_confidence==="possible"&&memory.duplicate_of_idea_id?Array.from(new Set([...(memory.related_idea_ids??[]),memory.duplicate_of_idea_id])):memory.related_idea_ids,possible_duplicate_of:memory.duplicate_confidence==="possible"?memory.duplicate_of_idea_id:null,agentic:true,toolbox_version:9,model:"gpt-5.6-luna",processing_status:"ready",enriched_at:now};const{error:updateError}=await supabase.from("ideas").update({title:memory.title,summary:memory.summary,tags:memory.tags,people:memory.people,location_label:memory.subject_location,location_source:memory.subject_location?(memory.subject_location_is_from_user?"extracted":"researched"):idea.latitude!=null?"device":null,enrichment}).eq("id",idea.id).eq("user_id",payload.userId);if(updateError)throw updateError;if(memory.assigned_category_id){await supabase.from("idea_categories").delete().eq("idea_id",idea.id);const{error:categoryError}=await supabase.from("idea_categories").insert({idea_id:idea.id,category_id:memory.assigned_category_id});if(categoryError)console.error("Category assignment failed",categoryError)}return{ideaId:idea.id,status:"ready"}}catch