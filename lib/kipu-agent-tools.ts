import type { SupabaseClient } from "@supabase/supabase-js";
import { compactIdentityCandidate, deterministicIdentityMatch, type IdentityInput } from "@/lib/kipu-duplicate-identity";

export type AgentToolResult = Record<string, unknown>;
export type AgentToolContext = { supabase?: SupabaseClient; userId?: string; openAiKey?: string; currentIdeaId?: string };
function absoluteUrl(value:string,base:string){try{return new URL(value,base).toString()}catch{return null}}
function getMeta(html:string,patterns:RegExp[]){for(const pattern of patterns){const match=html.match(pattern);if(match?.[1])return match[1].replace(/&amp;/g,"&").trim()}return null}
function textOf(p:any){if(typeof p.output_text==="string")return p.output_text;for(const o of p.output??[])for(const c of o.content??[])if(typeof c?.text==="string")return c.text;return null}
function confidenceRank(value:any){return value==="high"?2:value==="possible"?1:0}

export async function inspectWebPage(url:string):Promise<AgentToolResult>{try{const response=await fetch(url,{redirect:"follow",headers:{"User-Agent":"Mozilla/5.0 KipuBot/0.2"},signal:AbortSignal.timeout(6500)});if(!response.ok)return{ok:false,url,status:response.status};const finalUrl=response.url||url,contentType=response.headers.get("content-type")||"";if(!contentType.includes("text/html"))return{ok:false,url:finalUrl,reason:"not_html",contentType};const html=(await response.text()).slice(0,850000);const title=getMeta(html,[/<meta[^>]+property=["']og:title["'][^>]+content=["']([^"']+)["']/i,/<meta[^>]+content=["']([^"']+)["'][^>]+property=["']og:title["']/i,/<title[^>]*>([^<]+)<\/title>/i]);const description=getMeta(html,[/<meta[^>]+property=["']og:description["'][^>]+content=["']([^"']+)["']/i,/<meta[^>]+name=["']description["'][^>]+content=["']([^"']+)["']/i,/<meta[^>]+content=["']([^"']+)["'][^>]+name=["']description["']/i]);const patterns=[/<meta[^>]+property=["']og:image(?::secure_url)?["'][^>]+content=["']([^"']+)["']/gi,/<meta[^>]+content=["']([^"']+)["'][^>]+property=["']og:image(?::secure_url)?["']/gi,/<meta[^>]+name=["']twitter:image(?::src)?["'][^>]+content=["']([^"']+)["']/gi,/<img[^>]+src=["']([^"']+)["'][^>]*>/gi];const images:string[]=[],seen=new Set<string>();for(const pattern of patterns){for(const match of html.matchAll(pattern)){const abs=match[1]?absoluteUrl(match[1].replace(/&amp;/g,"&"),finalUrl):null;if(abs?.startsWith("http")&&!seen.has(abs)){seen.add(abs);images.push(abs);if(images.length>=12)break}}if(images.length>=12)break}const structuredData=Array.from(html.matchAll(/<script[^>]+type=["']application\/ld\+json["'][^>]*>([\s\S]*?)<\/script>/gi)).slice(0,4).map(m=>m[1].replace(/\s+/g," ").trim().slice(0,8000));return{ok:true,url:finalUrl,title,description,images,structuredData}}catch(error){return{ok:false,url,error:error instanceof Error?error.message:"fetch_failed"}}}
export async function reverseGeocode(latitude:number,longitude:number):Promise<AgentToolResult>{try{const endpoint=new URL("https://nominatim.openstreetmap.org/reverse");endpoint.searchParams.set("format","jsonv2");endpoint.searchParams.set("lat",String(latitude));endpoint.searchParams.set("lon",String(longitude));endpoint.searchParams.set("zoom","18");endpoint.searchParams.set("addressdetails","1");const response=await fetch(endpoint,{headers:{"User-Agent":"Kipu/0.2 personal-memory-app","Accept-Language":"de,en;q=0.8"},signal:AbortSignal.timeout(5000)});if(!response.ok)return{ok:false,status:response.status};const data=await response.json();return{ok:true,displayName:data.display_name??null,address:data.address??null,type:data.type??null,category:data.category??null,latitude,longitude}}catch(error){return{ok:false,error:error instanceof Error?error.message:"reverse_geocode_failed"}}}
export async function searchPlaces(query:string,latitude?:number,longitude?:number):Promise<AgentToolResult>{try{const endpoint=new URL("https://nominatim.openstreetmap.org/search");endpoint.searchParams.set("format","jsonv2");endpoint.searchParams.set("q",query);endpoint.searchParams.set("addressdetails","1");endpoint.searchParams.set("limit","8");endpoint.searchParams.set("namedetails","1");endpoint.searchParams.set("extratags","1");if(Number.isFinite(latitude)&&Number.isFinite(longitude)){const span=.35;endpoint.searchParams.set("viewbox",`${Number(longitude)-span},${Number(latitude)+span},${Number(longitude)+span},${Number(latitude)-span}`)}const response=await fetch(endpoint,{headers:{"User-Agent":"Kipu/0.2 personal-memory-app","Accept-Language":"de,en;q=0.8"},signal:AbortSignal.timeout(5500)});if(!response.ok)return{ok:false,status:response.status};const rows=await response.json();return{ok:true,results:rows.map((r:any)=>({name:r.namedetails?.name??r.name??r.display_name?.split(",")[0]??null,displayName:r.display_name,latitude:Number(r.lat),longitude:Number(r.lon),type:r.type,category:r.category,address:r.address,website:r.extratags?.website??r.extratags?.["contact:website"]??null,phone:r.extratags?.phone??r.extratags?.["contact:phone"]??null,openingHours:r.extratags?.opening_hours??null})).slice(0,8)}}catch(error){return{ok:false,error:error instanceof Error?error.message:"place_search_failed"}}}
async function callSearch(openAiKey:string,query:string){const response=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{Authorization:`Bearer ${openAiKey}`,"Content-Type":"application/json"},body:JSON.stringify({model:"gpt-5.6-luna",tools:[{type:"web_search"}],include:["web_search_call.action.sources"],input:`Find highly relevant public webpages for this visual subject. Prefer official/entity pages and reputable pages likely to contain representative images. Subject: ${query}`})});if(!response.ok)return[] as Array<{title?:string;url:string}>;const payload=await response.json(),sources:Array<{title?:string;url:string}>=[],seen=new Set<string>();for(const o of payload.output??[])if(o.type==="web_search_call")for(const s of o.action?.sources??[])if(s?.url&&!seen.has(s.url)){seen.add(s.url);sources.push({title:s.title,url:s.url})}return sources.slice(0,8)}
export async function searchImages(query:string,context:AgentToolContext):Promise<AgentToolResult>{if(!context.openAiKey)return{ok:false,error:"missing_openai_key"};const pages=await callSearch(context.openAiKey,query),inspected=await Promise.all(pages.slice(0,6).map(async p=>({source:p,page:await inspectWebPage(p.url)}))),candidates:Array<{url:string;sourceUrl:string;sourceTitle?:string}>=[],seen=new Set<string>();for(const item of inspected){const imgs=Array.isArray(item.page.images)?item.page.images as string[]:[];for(const image of imgs)if(!seen.has(image)){seen.add(image);candidates.push({url:image,sourceUrl:item.source.url,sourceTitle:item.source.title});if(candidates.length>=20)break}if(candidates.length>=20)break}return{ok:true,query,pages,candidates}}
export async function searchMyIdeas(query:string,context:AgentToolContext):Promise<AgentToolResult>{if(!context.supabase||!context.userId)return{ok:false,error:"missing_memory_context"};const safe=query.trim().replace(/[,%()]/g," ");if(!safe)return{ok:true,results:[]};const{data,error}=await context.supabase.from("ideas").select("id,title,summary,original_input,tags,people,location_label,created_at,enrichment").eq("user_id",context.userId).or(`title.ilike.%${safe}%,summary.ilike.%${safe}%,original_input.ilike.%${safe}%`).order("created_at",{ascending:false}).limit(10);if(error)return{ok:false,error:error.message};return{ok:true,results:data??[]}}

const duplicateSchema={type:"object",additionalProperties:false,properties:{matches:{type:"array",maxItems:8,items:{type:"object",additionalProperties:false,properties:{idea_id:{type:"string"},confidence:{type:"string",enum:["none","possible","high"]},reason:{type:"string"}},required:["idea_id","confidence","reason"]}}},required:["matches"]};
export async function findSimilarIdeas(args:any,context:AgentToolContext):Promise<AgentToolResult>{if(!context.supabase||!context.userId)return{ok:false,error:"missing_memory_context"};const currentIdeaId=String(args.current_idea_id||context.currentIdeaId||""),current:IdentityInput={id:currentIdeaId,title:args.canonical_name||args.text,summary:args.summary,original_input:args.text,tags:args.tags,people:args.people,location_label:args.location_label,canonical_name:args.canonical_name,urls:args.urls,identifiers:args.identifiers,latitude:args.latitude,longitude:args.longitude};const{data,error}=await context.supabase.from("ideas").select("id,title,summary,original_input,tags,people,location_label,created_at,enrichment").eq("user_id",context.userId).order("created_at",{ascending:false}).limit(120);if(error)return{ok:false,error:error.message};const rows=(data??[]).filter((x:any)=>x.id!==currentIdeaId&&x.enrichment?.processing_status!=="merged"),deterministic=rows.map((row:any)=>({row,match:deterministicIdentityMatch(current,row)})).filter(x=>x.match.confidence!=="none").sort((a,b)=>b.match.score-a.match.score),hard=deterministic.filter(x=>x.match.confidence==="high");if(hard.length){const results=hard.slice(0,8).map(x=>({idea_id:x.row.id,title:x.row.title,confidence:"high",score:x.match.score,matchers:x.match.matchers,reason:x.match.reasons.join("; "),decision_source:"deterministic"}));console.info("Kipu duplicate identity",{ideaId:currentIdeaId,candidates:results.map(x=>({id:x.idea_id,matchers:x.matchers,confidence:x.confidence,score:x.score}))});return{ok:true,results,best:results[0]}}if(!context.openAiKey){const results=deterministic.slice(0,8).map(x=>({idea_id:x.row.id,title:x.row.title,confidence:x.match.confidence,score:x.match.score,matchers:x.match.matchers,reason:x.match.reasons.join("; "),decision_source:"deterministic"}));return{ok:true,results,best:results[0]??null}}const candidates=rows.slice(0,100).map((row:any)=>compactIdentityCandidate(row)),response=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{Authorization:`Bearer ${context.openAiKey}`,"Content-Type":"application/json"},body:JSON.stringify({model:"gpt-5.6-luna",input:[{role:"system",content:[{type:"input_text",text:"Du bist Kipus generischer Entity-Identity-Matcher. Entscheide, ob die neue Erinnerung dieselbe konkrete Sache meint wie eine bestehende Erinnerung – unabhängig von Formulierung, persönlichem Kontext oder Eingabekanal. Trenne Entity Identity (was ist die Sache?) strikt von persönlicher Absicht (warum merken?). Gleicher Autor, gleiche Kategorie, gleiche Marke oder ähnliche Idee allein reichen nicht. Zwei Filialen einer Kette sind verschiedene Entitäten. Varianten/ähnliche Produkte konservativ behandeln. high nur wenn sehr sicher dieselbe konkrete Entität, possible wenn plausibel aber nicht eindeutig, sonst none. Nutze strukturierte Fakten, Links, Identifikatoren, Orte, Personen und vorhandenen Memory-Index. Gib höchstens 8 relevante Kandidaten zurück."}]},{role:"user",content:[{type:"input_text",text:JSON.stringify({current:compactIdentityCandidate(current),candidates})}]}],text:{format:{type:"json_schema",name:"kipu_duplicate_identity",strict:true,schema:duplicateSchema}}})});if(!response.ok)return{ok:false,error:`duplicate_semantic_${response.status}`};const raw=textOf(await response.json());if(!raw)return{ok:true,results:[],best:null};const parsed=JSON.parse(raw),valid=new Map(rows.map((x:any)=>[x.id,x])),results=(parsed.matches??[]).filter((m:any)=>m.idea_id!==currentIdeaId&&valid.has(m.idea_id)&&m.confidence!=="none").map((m:any)=>{const det=deterministic.find(x=>x.row.id===m.idea_id)?.match;return{idea_id:m.idea_id,title:valid.get(m.idea_id)?.title,confidence:m.confidence,score:det?.score??null,matchers:det?.matchers??["semantic_identity"],reason:m.reason,decision_source:"semantic"}}).sort((a:any,b:any)=>confidenceRank(b.confidence)-confidenceRank(a.confidence));console.info("Kipu duplicate identity",{ideaId:currentIdeaId,candidates:results.map((x:any)=>({id:x.idea_id,matchers:x.matchers,confidence:x.confidence,source:x.decision_source}))});return{ok:true,results:results.slice(0,8),best:results[0]??null}}

export async function getCategories(context:AgentToolContext):Promise<AgentToolResult>{if(!context.supabase||!context.userId)return{ok:false,error:"missing_memory_context"};const{data:categories,error}=await context.supabase.from("categories").select("id,parent_id,name,description,sort_order,created_at").eq("user_id",context.userId).order("sort_order").order("name");if(error)return{ok:false,error:error.message};const{data:assignments,error:assignmentError}=await context.supabase.from("idea_categories").select("category_id");if(assignmentError)return{ok:false,error:assignmentError.message};const counts=new Map<string,number>();for(const row of assignments??[])counts.set(row.category_id,(counts.get(row.category_id)??0)+1);const rows=(categories??[]).map((c:any)=>({...c,idea_count:counts.get(c.id)??0})),mains=rows.filter((c:any)=>!c.parent_id).map((c:any)=>({...c,children:rows.filter((x:any)=>x.parent_id===c.id)}));return{ok:true,total:rows.length,main_count:mains.length,categories:mains}}
export async function manageCategories(action:string,args:Record<string,unknown>,context:AgentToolContext):Promise<AgentToolResult>{if(!context.supabase||!context.userId)return{ok:false,error:"missing_memory_context"};const supabase=context.supabase,userId=context.userId;if(action==="create"){const name=String(args.name??"").trim();if(!name)return{ok:false,error:"name_required"};const parentId=args.parent_id?String(args.parent_id):null;if(parentId){const{data:parent,error}=await supabase.from("categories").select("id,parent_id").eq("id",parentId).eq("user_id",userId).single();if(error||!parent)return{ok:false,error:"invalid_parent"};if(parent.parent_id)return{ok:false,error:"max_depth_two"}}const{data,error}=await supabase.from("categories").insert({user_id:userId,parent_id:parentId,name,description:args.description?String(args.description):null}).select("id,parent_id,name,description").single();if(error)return{ok:false,error:error.message};return{ok:true,category:data}}if(action==="update"){const categoryId=String(args.category_id??"");if(!categoryId)return{ok:false,error:"category_id_required"};const patch:Record<string,unknown>={};if(args.name!=null)patch.name=String(args.name).trim();if(args.description!==undefined)patch.description=args.description?String(args.description):null;if(args.parent_id!==undefined)patch.parent_id=args.parent_id?String(args.parent_id):null;const{data,error}=await supabase.from("categories").update(patch).eq("id",categoryId).eq("user_id",userId).select("id,parent_id,name,description").single();if(error)return{ok:false,error:error.message};return{ok:true,category:data}}if(action==="delete"){const categoryId=String(args.category_id??"");if(!categoryId)return{ok:false,error:"category_id_required"};const{error}=await supabase.from("categories").delete().eq("id",categoryId).eq("user_id",userId);if(error)return{ok:false,error:error.message};return{ok:true,deleted:categoryId}}return{ok:false,error:"unsupported_action"}}

export const kipuFunctionTools=[
{type:"function",name:"inspect_web_page",description:"Inspect a specific public webpage and return metadata, structured data and image candidates.",strict:true,parameters:{type:"object",additionalProperties:false,properties:{url:{type:"string"}},required:["url"]}},
{type:"function",name:"search_images",description:"Search generically for representative image candidates for any remembered subject.",strict:true,parameters:{type:"object",additionalProperties:false,properties:{query:{type:"string"}},required:["query"]}},
{type:"function",name:"search_places",description:"Search public place/POI data by natural-language query, optionally biased near coordinates.",strict:true,parameters:{type:"object",additionalProperties:false,properties:{query:{type:"string"},latitude:{anyOf:[{type:"number"},{type:"null"}]},longitude:{anyOf:[{type:"number"},{type:"null"}]}},required:["query","latitude","longitude"]}},
{type:"function",name:"reverse_geocode",description:"Resolve GPS coordinates into a human-readable place. Capture location is not automatically subject location.",strict:true,parameters:{type:"object",additionalProperties:false,properties:{latitude:{type:"number"},longitude:{type:"number"}},required:["latitude","longitude"]}},
{type:"function",name:"search_my_ideas",description:"Search the user's existing Kipu memories when prior saved context could help interpret a reference.",strict:true,parameters:{type:"object",additionalProperties:false,properties:{query:{type:"string"}},required:["query"]}},
{type:"function",name:"find_similar_ideas",description:"Generic entity-identity duplicate search. Finds the same concrete thing across different wording, context or input channels using hard identifiers, structured data, location and semantic identity.",strict:true,parameters:{type:"object",additionalProperties:false,properties:{text:{type:"string"},current_idea_id:{type:"string"},canonical_name:{anyOf:[{type:"string"},{type:"null"}]},summary:{anyOf:[{type:"string"},{type:"null"}]},tags:{type:"array",items:{type:"string"}},people:{type:"array",items:{type:"string"}},location_label:{anyOf:[{type:"string"},{type:"null"}]},urls:{type:"array",items:{type:"string"}},identifiers:{type:"array",items:{type:"object",additionalProperties:false,properties:{type:{type:"string"},value:{type:"string"}},required:["type","value"]}},latitude:{anyOf:[{type:"number"},{type:"null"}]},longitude:{anyOf:[{type:"number"},{type:"null"}]}},required:["text","current_idea_id","canonical_name","summary","tags","people","location_label","urls","identifiers","latitude","longitude"]}},
{type:"function",name:"get_categories",description:"Read the user's current self-organized category tree including idea counts.",strict:true,parameters:{type:"object",additionalProperties:false,properties:{},required:[]}},
{type:"function",name:"manage_categories",description:"Create or maintain the user's category taxonomy.",strict:true,parameters:{type:"object",additionalProperties:false,properties:{action:{type:"string",enum:["create","update","delete"]},category_id:{anyOf:[{type:"string"},{type:"null"}]},parent_id:{anyOf:[{type:"string"},{type:"null"}]},name:{anyOf:[{type:"string"},{type:"null"}]},description:{anyOf:[{type:"string"},{type:"null"}]}},required:["action","category_id","parent_id","name","description"]}}
] as const;
export async function runKipuFunctionTool(name:string,args:Record<string,unknown>,context:AgentToolContext={}){if(name==="inspect_web_page")return inspectWebPage(String(args.url??""));if(name==="search_images")return searchImages(String(args.query??""),context);if(name==="search_places")return searchPlaces(String(args.query??""),args.latitude==null?undefined:Number(args.latitude),args.longitude==null?undefined:Number(args.longitude));if(name==="reverse_geocode")return reverseGeocode(Number(args.latitude),Number(args.longitude));if(name==="search_my_ideas")return searchMyIdeas(String(args.query??""),context);if(name==="find_similar_ideas")return findSimilarIdeas(args,context);if(name==="get_categories")return getCategories(context);if(name==="manage_categories")return manageCategories(String(args.action??""),args,context);return{ok:false,error:`unknown_tool:${name}`}}
