import type { EnrichIdeaPayload } from "@/lib/kipu-enrichment";
async function enrichIdeaStep(payload:EnrichIdeaPayload){"use step";const{enrichPendingIdea}=await import("@/lib/kipu-enrichment");return enrichPendingIdea(payload)}
async function ensureCategoryStep(payload:EnrichIdeaPayload,ideaId:string){"use step";const{createClient}=await import("@supabase/supabase-js");const{ensureIdeaCategory}=await import("@/lib/kipu-category-manager");const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,openAiKey=process.env.OPENAI_API_KEY;if(!url||!key||!openAiKey)throw new Error("missing_category_configuration");const s=createClient(url,key,{global:{headers:{Authorization:`Bearer ${payload.accessToken}`}},auth:{persistSession:false,autoRefreshToken:false}}),{data:idea,error}=await s.from("ideas").select("id,title,summary,tags,enrichment").eq("id",ideaId).eq("user_id",payload.userId).single();if(error||!idea)throw new Error(error?.message||"category_idea_not_found");return ensureIdeaCategory(s,payload.userId,openAiKey,idea)}
async function indexMemoryStep(payload:EnrichIdeaPayload,ideaId:string){"use step";const{createClient}=await import("@supabase/supabase-js");const{indexIdeaMemory}=await import("@/lib/kipu-memory-index");const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,openAiKey=process.env.OPENAI_API_KEY;if(!url||!key||!openAiKey)throw new Error("missing_memory_configuration");const s=createClient(url,key,{global:{headers:{Authorization:`Bearer ${payload.accessToken}`}},auth:{persistSession:false,autoRefreshToken:false}});return indexIdeaMemory(s,payload.userId,openAiKey,ideaId)}
async function graphMemoryStep(payload:EnrichIdeaPayload,ideaId:string){"use step";const{createClient}=await import("@supabase/supabase-js");const{buildMemoryRelations}=await import("@/lib/kipu-memory-graph");const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,openAiKey=process.env.OPENAI_API_KEY;if(!url||!key||!openAiKey)throw new Error("missing_graph_configuration");const s=createClient(url,key,{global:{headers:{Authorization:`Bearer ${payload.accessToken}`}},auth:{persistSession:false,autoRefreshToken:false}});return buildMemoryRelations(s,payload.userId,openAiKey,ideaId)}
export async function enrichIdeaWorkflow(payload:EnrichIdeaPayload){"use workflow";const result=await enrichIdeaStep(payload);await ensureCategoryStep(payload,result.ideaId);await indexMemoryStep(payload,result.ideaId);await graphMemoryStep(payload,result.ideaId);return result}
